import React from 'react';
import './CheckerBoard.css';

const CheckerBoard = ({ size }) => {
    console.log(size);

    return (
        <div className="grid">
            
        </div>
    );
};

export default CheckerBoard;